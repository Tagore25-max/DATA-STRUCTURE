graphs is a non linear data structure which is used to represents the relationship between

graph terminologies:
1.Node:
   it contains actual data.
   it is also called as vertices.
   it represented by 
   
   adjacent Nodes:
        two vertices are adjacents if they are connected by an edge.
		  ex: 
		            A
		           / \
		          B___C
		           
	degree of a node:
		no.of edges connected to the node.
	size of a graph:
		total no.of edges in a graph.
		
		directional graph:
			it is also called as unidirectional graph.
		undirectional graph:
			edges are no directional.
		    it is also called as bidirectional graph.
		weighted graph:
			in this graph edges have some weight.
		unweighted graph:
			edges have no weight.
		cyclic graph:
			it is a type of graph that contains atleast one loop.
		acyclic graph:
			it is a type of graph it doesn't contain any loop.
		complete graph:
			a graph were every pair of vertices were connected to each other.
		
		
